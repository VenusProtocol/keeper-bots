query TokenConverter ($id: ID!) {
  tokenConverters(where: { id: $id} ) {
    id
    destinationAddress
    baseAsset
    configs(where:{  or: [{access:ALL}, {access:ONLY_FOR_USERS}]}) {
      id
      tokenAddressIn
      tokenAddressOut
      incentive
      access
    }
  }
}
