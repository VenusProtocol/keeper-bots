query TokenConverters {
  tokenConverterConfigs(
       where: { or: [{ access: ALL }, { access: ONLY_FOR_USERS }] }
    ) {
    id
    tokenAddressIn
    tokenAddressOut
    incentive
    access
    tokenConverter {
      id
    }
  }
}
