query TokenConverterConfigsByAssetInAndAssetOut($tokenAddressIn: Bytes!, $tokenAddressOut: Bytes!) {
  tokenConverterConfigs(
       where: {
        and: [
          { tokenAddressIn: $tokenAddressIn }
          { tokenAddressOut: $tokenAddressOut }
          { or: [{ access: ALL }, { access: ONLY_FOR_USERS }] }
        ]
      }
    ) {
    id
    tokenAddressIn
    tokenAddressOut
    incentive
    access
    tokenConverter {
      id
    }
  }
}