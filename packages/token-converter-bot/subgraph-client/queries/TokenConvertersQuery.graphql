query TokenConverters {
  tokenConverters(where: { paused: false} ) {
    id
    converterNetwork {
      id 
    }
    destinationAddress
    baseAsset
    configs {
      id
      tokenAddressIn
      tokenAddressOut
      incentive
      access
    }
  }
}
